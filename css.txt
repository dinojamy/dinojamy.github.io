:root {
    --background: #f5f7fb;
    --surface: #ffffff;
    --surface-soft: #eaf0ff;
    --surface-muted: #f3f5fa;
    --text: #1b2340;
    --text-soft: #6b7280;
    --line: #e4e8f2;
    --primary: #3a5ce0;
    --primary-dark: #2d48b8;
    --primary-soft: #e9edff;
    --accent: #ff8a5b;
    --accent-soft: #fff0e6;
    --success: #21a179;
    --success-soft: #e6f8f1;
    --danger: #e5484d;
    --danger-soft: #fff0f0;
    --warning: #f1a33c;
    --shadow: 0 18px 50px rgba(24, 32, 64, 0.09);
    --shadow-soft: 0 10px 30px rgba(24, 32, 64, 0.07);
    --nav-background: rgba(255, 255, 255, 0.88);
    font-family: Pretendard, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    color: var(--text);
    background: var(--background);
    font-synthesis: none;
}

:root[data-theme="dark"] {
    --background: #10131f;
    --surface: #1a1e2e;
    --surface-soft: #232a48;
    --surface-muted: #1e2333;
    --text: #f2f4fb;
    --text-soft: #a2a8bd;
    --line: #2c3348;
    --primary: #7d93ff;
    --primary-dark: #6580f0;
    --primary-soft: #262d4d;
    --accent: #ff9f78;
    --accent-soft: #392a24;
    --success: #45c996;
    --success-soft: #16352b;
    --danger: #ff7378;
    --danger-soft: #3b2428;
    --shadow: 0 22px 60px rgba(0, 0, 0, 0.3);
    --shadow-soft: 0 12px 32px rgba(0, 0, 0, 0.24);
    --nav-background: rgba(26, 30, 46, 0.9);
    color-scheme: dark;
}

* { box-sizing: border-box; }
html, body { min-height: 100%; margin: 0; background: var(--background); }
body { min-width: 320px; -webkit-tap-highlight-color: transparent; }
button, input { font: inherit; }
button { border: 0; cursor: pointer; }
button:focus-visible, input:focus-visible { outline: 3px solid rgba(58, 92, 224, 0.24); outline-offset: 2px; }

.app-shell { position: relative; width: 100%; min-height: 100dvh; overflow: hidden; background: var(--background); }
.screen { display: none; min-height: 100dvh; animation: screen-in 280ms ease; }
.screen.active { display: flex; }
@keyframes screen-in { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }

.login-screen {
    position: relative; align-items: flex-end;
    padding: max(48px, env(safe-area-inset-top)) 24px max(28px, env(safe-area-inset-bottom));
    overflow: hidden;
    background: radial-gradient(circle at 20% 4%, rgba(255, 138, 91, 0.2), transparent 28%), linear-gradient(160deg, #f8faff 0%, #edf1ff 46%, #fff 46%);
}
.login-content { position: relative; z-index: 2; width: 100%; max-width: 430px; margin: 0 auto; }
.brand-mark { display: grid; width: 56px; height: 56px; margin-bottom: 22px; place-items: center; border-radius: 18px; color: #fff; background: var(--primary); box-shadow: 0 12px 28px rgba(58, 92, 224, 0.3); font-family: Georgia, serif; font-size: 25px; font-weight: 800; }
.eyebrow { margin: 0 0 10px; color: var(--primary); font-size: 14px; font-weight: 750; letter-spacing: -0.02em; }
.login-content h1, .mode-content h1, .result-content h1 { margin: 0; font-size: clamp(30px, 8vw, 38px); line-height: 1.25; letter-spacing: -0.03em; }
.login-form { display: grid; gap: 14px; margin-top: 34px; }
.input-field { display: grid; gap: 8px; }
.input-field > span:first-child { color: #4e5968; font-size: 13px; font-weight: 700; }
.input-field input { width: 100%; height: 58px; padding: 0 17px; border: 1px solid var(--line); border-radius: 16px; color: var(--text); background: rgba(255, 255, 255, 0.94); box-shadow: 0 4px 18px rgba(31, 45, 72, 0.035); font-size: 16px; transition: border-color 160ms ease, box-shadow 160ms ease; }
.input-field input:focus { border-color: var(--primary); outline: 0; box-shadow: 0 0 0 4px rgba(58, 92, 224, 0.12); }
.password-wrap { position: relative; display: block; }
.password-wrap input { padding-right: 66px; }
.text-button { position: absolute; top: 50%; right: 10px; padding: 9px; transform: translateY(-50%); color: var(--primary); background: transparent; font-size: 13px; font-weight: 750; }
.form-error { min-height: 19px; margin: -2px 2px 0; color: var(--danger); font-size: 13px; font-weight: 650; }
.primary-button, .secondary-button { width: 100%; min-height: 58px; padding: 0 20px; border-radius: 17px; font-size: 16px; font-weight: 800; transition: transform 120ms ease, background 120ms ease; }
.primary-button { color: #fff; background: var(--primary); box-shadow: 0 12px 26px rgba(58, 92, 224, 0.26); }
.primary-button:active, .secondary-button:active, .mode-card:active, .lesson-card:active, .answer-button:active { transform: scale(0.985); }
.primary-button:hover { background: var(--primary-dark); }
.secondary-button { color: var(--text); background: var(--surface-muted); }
.login-actions { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
.login-actions .primary-button, .login-actions .secondary-button { min-height: 54px; border-radius: 14px; font-size: 15px; }
.login-actions button:disabled { cursor: wait; opacity: 0.58; }

.invite-screen { min-height: 100dvh; align-items: center; justify-content: center; padding: max(42px, env(safe-area-inset-top)) 24px max(28px, env(safe-area-inset-bottom)); }
.invite-content { width: min(100%, 430px); margin: 0 auto; }
.invite-content h1 { margin: 0; font-size: clamp(30px, 8vw, 38px); line-height: 1.25; }
.invite-form { display: grid; gap: 14px; margin-top: 26px; }
.invite-form input { text-transform: uppercase; }
.text-link-button { width: 100%; min-height: 46px; margin-top: 12px; color: var(--text-soft); background: transparent; font-size: 13px; font-weight: 750; }

.login-decoration { position: absolute; inset: 0; pointer-events: none; }
.floating-card { position: absolute; display: grid; width: 62px; height: 74px; place-items: center; border: 1px solid rgba(255, 255, 255, 0.8); border-radius: 18px; color: var(--primary); background: rgba(255, 255, 255, 0.6); box-shadow: var(--shadow); backdrop-filter: blur(8px); font-family: Georgia, serif; font-size: 29px; font-weight: 700; }
.card-one { top: 9%; right: 13%; transform: rotate(9deg); }
.card-two { top: 19%; right: 38%; transform: rotate(-11deg) scale(0.84); }
.card-three { top: 5%; left: 11%; transform: rotate(-6deg) scale(0.76); }

.home-screen { flex-direction: column; padding: max(24px, env(safe-area-inset-top)) 20px calc(110px + env(safe-area-inset-bottom)); background: radial-gradient(circle at 92% 0%, rgba(58, 92, 224, 0.13), transparent 27%), radial-gradient(circle at 3% 24%, rgba(255, 138, 91, 0.12), transparent 22%), var(--background); }
.top-bar { display: flex; align-items: center; justify-content: space-between; margin-bottom: 26px; }
.home-actions { display: flex; align-items: center; gap: 8px; }
.profile-summary-card { display: flex; align-items: center; gap: 12px; margin-bottom: 12px; padding: 15px; border: 1px solid var(--line); border-radius: 22px; background: var(--surface); box-shadow: var(--shadow-soft); }
.profile-avatar, .rank-avatar { display: grid; flex: 0 0 auto; width: 48px; height: 48px; place-items: center; border-radius: 16px; color: #fff; background: var(--primary); font-family: Georgia, serif; font-size: 22px; font-weight: 800; }
.profile-summary-copy { display: grid; min-width: 0; flex: 1; gap: 2px; }
.profile-summary-copy > span { color: var(--text-soft); font-size: 10px; font-weight: 700; }
.profile-summary-copy > strong { overflow: hidden; font-size: 16px; text-overflow: ellipsis; white-space: nowrap; }
.profile-summary-copy > small { color: var(--primary); font-size: 11px; font-weight: 750; }
.level-up-button { display: grid; flex: 0 0 auto; min-width: 72px; min-height: 50px; place-items: center; gap: 1px; padding: 7px 10px; border-radius: 15px; color: #fff; background: var(--primary); }
.level-up-button span { font-size: 10px; font-weight: 750; }
.level-up-button strong { font-size: 12px; }
.level-up-button:disabled { cursor: not-allowed; color: var(--text-soft); background: var(--surface-muted); }

.attendance-card { display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 11px; margin-bottom: 18px; padding: 13px 14px; border: 1px solid rgba(241, 163, 60, 0.2); border-radius: 20px; background: #fff8e8; }
.attendance-icon { display: grid; width: 39px; height: 39px; place-items: center; border-radius: 13px; color: #a56613; background: #ffedc5; }
.attendance-icon svg { width: 21px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
.attendance-card > div { display: grid; min-width: 0; gap: 3px; }
.attendance-card strong { font-size: 13px; }
.attendance-card span { color: #8a6c3a; font-size: 10px; }
.attendance-card button { min-height: 38px; padding: 0 11px; border-radius: 12px; color: #fff; background: #b87519; font-size: 11px; font-weight: 850; }
.attendance-card button:disabled { color: #8a6c3a; background: #f1dfba; }

.point-pill { display: flex; height: 44px; align-items: center; gap: 7px; padding: 0 12px 0 8px; border: 1px solid rgba(241, 163, 60, 0.18); border-radius: 15px; color: #8a5b13; background: #fff8e8; box-shadow: var(--shadow-soft); font-size: 13px; }
.point-pill strong { font-size: 15px; }
.point-coin { display: grid; width: 27px; height: 27px; place-items: center; border-radius: 50%; color: #fff; background: linear-gradient(145deg, #ffc65e, #ed9b2d); box-shadow: inset 0 -2px 0 rgba(119, 70, 4, 0.16); font-size: 12px; font-weight: 900; }
.point-coin.small { width: 20px; height: 20px; font-size: 9px; }
.top-caption { margin: 0 0 4px; color: var(--primary); font-size: 13px; font-weight: 750; }
.top-bar h1 { margin: 0; max-width: 270px; font-size: 24px; letter-spacing: -0.03em; line-height: 1.25; }
.icon-button, .back-button, .close-button { display: grid; flex: 0 0 auto; width: 44px; height: 44px; place-items: center; border-radius: 14px; color: var(--text); background: var(--surface); box-shadow: var(--shadow-soft); }
.icon-button svg, .back-button svg, .close-button svg { width: 22px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

.textbook-card { position: relative; display: grid; grid-template-columns: 112px 1fr; gap: 18px; min-height: 174px; padding: 16px; overflow: hidden; border: 1px solid rgba(58, 92, 224, 0.12); border-radius: 30px; background: radial-gradient(circle at 95% 0%, rgba(255, 138, 91, 0.16), transparent 35%), linear-gradient(145deg, var(--surface), var(--surface-soft)); box-shadow: var(--shadow); }
.textbook-card::after { position: absolute; right: -24px; bottom: -30px; width: 110px; height: 110px; border: 18px solid rgba(58, 92, 224, 0.07); border-radius: 50%; content: ""; }
.book-cover-wrap { position: relative; z-index: 1; align-self: center; padding: 5px; border-radius: 11px 16px 16px 11px; background: #fff; box-shadow: -5px 8px 18px rgba(29, 40, 96, 0.14), inset 4px 0 0 #d8dfe8; transform: rotate(-2deg); }
.book-cover { display: block; width: 100%; aspect-ratio: 0.777; border-radius: 7px 11px 11px 7px; object-fit: cover; }
.book-cover-placeholder { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 6px; background: linear-gradient(160deg, var(--primary) 0%, var(--primary-dark) 100%); color: #fff; text-align: center; }
.book-cover-placeholder span { font-family: Georgia, serif; font-size: 20px; font-weight: 800; letter-spacing: 0.06em; }
.book-cover-placeholder em { font-style: normal; font-size: 9px; opacity: 0.82; }
.book-meta { position: relative; z-index: 1; display: flex; min-width: 0; flex-direction: column; justify-content: center; }
.book-label { align-self: flex-start; padding: 5px 9px; border-radius: 99px; color: var(--primary); background: var(--primary-soft); font-size: 11px; font-weight: 800; }
.book-meta h2 { margin: 10px 0 5px; font-size: 21px; letter-spacing: -0.03em; }
.book-meta p { margin: 0; color: var(--text-soft); font-size: 12px; line-height: 1.5; }
.book-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 13px; }
.book-tags span { padding: 5px 8px; border: 1px solid var(--line); border-radius: 8px; color: var(--text-soft); background: color-mix(in srgb, var(--surface) 82%, transparent); font-size: 10px; font-weight: 700; }

.streak-card { display: flex; gap: 14px; align-items: center; margin-top: 14px; padding: 18px; border-radius: 22px; border: 1px solid rgba(33, 161, 121, 0.1); color: var(--text); background: linear-gradient(135deg, var(--success-soft), var(--surface)); box-shadow: var(--shadow-soft); }
.streak-card > div:last-child { display: grid; gap: 4px; }
.streak-card strong { font-size: 15px; }
.streak-card span { color: var(--text-soft); font-size: 12px; line-height: 1.45; }
.streak-icon { display: grid; flex: 0 0 auto; width: 46px; height: 46px; place-items: center; border-radius: 15px; color: var(--success); background: rgba(255, 255, 255, 0.72); }
.streak-icon svg { width: 25px; fill: none; stroke: currentColor; stroke-width: 1.9; stroke-linecap: round; stroke-linejoin: round; }

.section-heading { display: flex; align-items: center; justify-content: space-between; margin: 30px 2px 15px; }
.section-heading h2 { margin: 0; font-size: 18px; }
.section-heading span { color: var(--text-soft); font-size: 13px; }
.lesson-list { display: grid; gap: 12px; }
.lesson-card { display: flex; width: 100%; align-items: center; gap: 15px; padding: 16px; border: 1px solid var(--line); border-radius: 24px; color: inherit; background: var(--surface); box-shadow: var(--shadow-soft); text-align: left; transition: transform 120ms ease, border-color 120ms ease; }
.lesson-card:hover { border-color: color-mix(in srgb, var(--primary) 35%, var(--line)); }
.lesson-card.locked { box-shadow: none; }
.lesson-card.locked:not(.purchasable) { opacity: 0.58; }
.lesson-card.purchasable { border-color: rgba(241, 163, 60, 0.3); background: linear-gradient(135deg, var(--surface), #fff9ed); }
.lesson-number { display: grid; flex: 0 0 auto; width: 52px; height: 52px; place-items: center; border-radius: 17px; color: var(--lesson-color); background: var(--lesson-bg); font-family: Georgia, serif; font-size: 22px; font-weight: 700; }
.lesson-info { display: grid; flex: 1; gap: 4px; }
.lesson-info strong { font-size: 16px; }
.lesson-title-row { display: flex; align-items: center; gap: 7px; }
.lesson-title-row em { padding: 3px 7px; border-radius: 99px; color: var(--primary); background: var(--primary-soft); font-size: 9px; font-style: normal; font-weight: 800; }
.lesson-info small { color: var(--text-soft); font-size: 12px; }
.lesson-status { display: flex; flex: 0 0 auto; min-width: 38px; height: 38px; align-items: center; justify-content: center; gap: 5px; border-radius: 13px; }
.lesson-status svg { width: 19px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
.lesson-status.ready { color: var(--primary); background: var(--primary-soft); }
.lesson-status.price { padding: 0 9px; color: #8a5b13; background: #fff7e5; font-size: 11px; font-weight: 850; }
.lesson-status.locked { color: var(--text-soft); background: var(--surface-muted); }
.lesson-card .chevron, .mode-card .chevron { width: 20px; fill: none; stroke: #b0b8c1; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

.content-screen { flex-direction: column; background: var(--background); }
.bottom-nav { position: fixed; z-index: 20; bottom: max(14px, env(safe-area-inset-bottom)); left: 50%; display: grid; width: min(calc(100% - 32px), 398px); grid-template-columns: repeat(4, 1fr); gap: 5px; padding: 7px; border: 1px solid color-mix(in srgb, var(--line) 75%, transparent); border-radius: 24px; background: var(--nav-background); box-shadow: 0 16px 40px rgba(20, 26, 55, 0.16); backdrop-filter: blur(18px); transform: translateX(-50%); }
.bottom-nav-item { display: grid; min-height: 52px; place-items: center; gap: 3px; border-radius: 18px; color: var(--text-soft); background: transparent; font-size: 11px; font-weight: 750; }
.bottom-nav-item.active { color: var(--primary); background: var(--primary-soft); }
.bottom-nav-item svg { width: 19px; fill: none; stroke: currentColor; stroke-width: 1.9; stroke-linecap: round; stroke-linejoin: round; }

.page-header { display: grid; grid-template-columns: 44px 1fr 44px; align-items: center; padding: max(14px, env(safe-area-inset-top)) 18px 10px; text-align: center; font-size: 15px; font-weight: 750; }
.settings-screen { position: relative; padding-bottom: calc(104px + env(safe-area-inset-bottom)); }
.settings-header { position: sticky; z-index: 4; top: 0; background: color-mix(in srgb, var(--background) 88%, transparent); backdrop-filter: blur(16px); }
.settings-content { display: grid; width: 100%; max-width: 520px; gap: 16px; margin: 0 auto; padding: 22px 20px 30px; }
.wallet-card { display: grid; grid-template-columns: auto 1fr; gap: 18px; align-items: center; padding: 22px; overflow: hidden; border-radius: 28px; color: #fff; background: radial-gradient(circle at 100% 0%, rgba(255, 255, 255, 0.18), transparent 40%), linear-gradient(135deg, #3a5ce0, #6b83f2); box-shadow: 0 18px 40px rgba(58, 92, 224, 0.28); }
.wallet-card > div:first-child { display: grid; gap: 3px; }
.wallet-card > div:first-child > span { color: rgba(255, 255, 255, 0.74); font-size: 11px; font-weight: 700; }
.wallet-card > div:first-child strong { font-size: 26px; letter-spacing: -0.03em; }
.wallet-card > div:first-child b { font-size: 36px; }
.wallet-progress { display: grid; gap: 5px; padding-left: 18px; border-left: 1px solid rgba(255, 255, 255, 0.22); }
.wallet-progress span { font-size: 14px; font-weight: 800; }
.wallet-progress small { color: rgba(255, 255, 255, 0.72); font-size: 11px; line-height: 1.45; }

.settings-group { display: grid; gap: 18px; padding: 21px; border: 1px solid var(--line); border-radius: 26px; background: var(--surface); box-shadow: var(--shadow-soft); }
.admin-invite-panel { border-color: color-mix(in srgb, var(--primary) 34%, var(--line)); }
.invite-code-box { display: grid; grid-template-columns: 1fr auto; gap: 10px; align-items: center; }
.invite-code-box strong { display: flex; min-width: 0; min-height: 48px; align-items: center; padding: 0 14px; border: 1px solid var(--line); border-radius: 12px; color: var(--primary); background: var(--primary-soft); font-size: 18px; font-weight: 900; }
.invite-code-box button { min-height: 48px; padding: 0 14px; border-radius: 12px; color: #fff; background: var(--primary); font-size: 12px; font-weight: 800; }
.profile-name-form { display: grid; grid-template-columns: 1fr auto; gap: 8px; }
.profile-name-form input { min-width: 0; min-height: 46px; padding: 0 14px; border: 1px solid var(--line); border-radius: 14px; color: var(--text); outline: none; background: var(--surface-muted); font: inherit; font-size: 13px; transition: border-color 140ms ease, box-shadow 140ms ease; }
.profile-name-form input:focus { border-color: var(--primary); box-shadow: 0 0 0 3px var(--primary-soft); }
.profile-name-form button { min-width: 64px; border-radius: 14px; color: #fff; background: var(--primary); font-size: 12px; font-weight: 800; }
.settings-group-heading { display: flex; align-items: end; justify-content: space-between; gap: 14px; }
.settings-group-heading h2 { margin: 3px 0 0; font-size: 18px; }
.settings-group-heading p { margin: 0; color: var(--text-soft); font-size: 11px; }
.settings-kicker { color: var(--primary); font-size: 10px; font-weight: 850; letter-spacing: 0.04em; }
.segmented-control, .compact-options { display: grid; gap: 5px; padding: 5px; border-radius: 17px; background: var(--surface-muted); }
.segmented-control { grid-template-columns: repeat(2, 1fr); }
.compact-options { grid-template-columns: repeat(3, 1fr); }
.segmented-control button, .compact-options button { min-height: 42px; padding: 0 7px; border-radius: 13px; color: var(--text-soft); background: transparent; font-size: 12px; font-weight: 750; }
.segmented-control button.selected, .compact-options button.selected { color: var(--primary); background: var(--surface); box-shadow: 0 5px 16px rgba(30, 36, 74, 0.09); }
.setting-choice { display: grid; gap: 13px; }
.setting-choice strong { font-size: 14px; }
.setting-choice small { color: var(--text-soft); font-size: 11px; line-height: 1.45; }
.compact-options button { min-height: 38px; font-size: 11px; }
.settings-logout { display: flex; min-height: 54px; align-items: center; justify-content: center; gap: 8px; border-radius: 18px; color: var(--danger); background: var(--danger-soft); font-size: 13px; font-weight: 800; }
.settings-logout svg { width: 19px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

/* ===== 상점 (인디고 잉크 테마) ===== */
.shop-screen { position: relative; padding-bottom: calc(104px + env(safe-area-inset-bottom)); overflow: hidden; color: #eef1ff; background: linear-gradient(180deg, rgba(13, 17, 43, 0.96), rgba(8, 10, 24, 1)), #080a18; }
.shop-screen::before { position: absolute; inset: 0; pointer-events: none; background: linear-gradient(rgba(125, 147, 255, 0.06) 1px, transparent 1px), linear-gradient(90deg, rgba(125, 147, 255, 0.05) 1px, transparent 1px); background-size: 34px 34px; mask-image: linear-gradient(to bottom, #000 0%, transparent 84%); content: ""; }
.shop-header { position: sticky; z-index: 4; top: 0; color: #eef1ff; background: rgba(8, 10, 24, 0.82); backdrop-filter: blur(16px); }
.shop-header .back-button { color: #a9b6ff; background: rgba(125, 147, 255, 0.1); box-shadow: 0 0 18px rgba(125, 147, 255, 0.14); }
.shop-content { position: relative; z-index: 1; display: grid; width: 100%; max-width: 520px; gap: 15px; margin: 0 auto; padding: 20px 18px 30px; }
.shop-wallet-panel { display: flex; align-items: center; justify-content: space-between; gap: 14px; padding: 18px; border: 1px solid rgba(125, 147, 255, 0.26); border-radius: 22px; background: rgba(17, 21, 51, 0.72); box-shadow: 0 0 0 1px rgba(180, 92, 255, 0.08) inset, 0 16px 42px rgba(0, 0, 0, 0.3); }
.shop-wallet-panel > div:first-child { display: grid; gap: 5px; }
.shop-wallet-panel span { color: rgba(238, 241, 255, 0.62); font-size: 11px; font-weight: 850; }
.shop-wallet-panel h1 { margin: 0; color: #eef1ff; font-size: 23px; }
.shop-balance-pill { display: inline-flex; width: fit-content; min-height: 36px; align-items: center; gap: 8px; padding: 7px 12px 7px 7px; border: 1px solid rgba(255, 211, 106, 0.34); border-radius: 99px; color: #ffe09a; background: rgba(255, 179, 92, 0.12); font-size: 13px; }
.shop-view-tabs { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 7px; padding: 6px; border: 1px solid rgba(125, 147, 255, 0.2); border-radius: 18px; background: rgba(5, 7, 20, 0.5); }
.shop-view-tabs button { min-height: 42px; border-radius: 13px; color: rgba(238, 241, 255, 0.62); background: transparent; font-size: 12px; font-weight: 900; }
.shop-view-tabs button.selected { color: #060a1f; background: #a9b6ff; box-shadow: 0 0 18px rgba(125, 147, 255, 0.3); }
.shop-panel[hidden] { display: none; }
.shop-summary-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; }
.shop-summary-grid > div { display: grid; gap: 5px; padding: 15px; border: 1px solid rgba(125, 147, 255, 0.2); border-radius: 18px; background: rgba(17, 21, 51, 0.72); box-shadow: 0 0 20px rgba(125, 147, 255, 0.06); }
.shop-summary-grid span { color: rgba(238, 241, 255, 0.62); font-size: 10px; font-weight: 800; }
.shop-summary-grid strong { overflow: hidden; color: #eef1ff; font-size: 15px; text-overflow: ellipsis; white-space: nowrap; }
.shop-section-heading h2 { color: #eef1ff; }
.shop-section-heading span { color: rgba(238, 241, 255, 0.58); }

.shop-item-list { display: grid; gap: 10px; }
.shop-item-card { position: relative; display: grid; grid-template-columns: 28px 68px minmax(0, 1fr) 118px; grid-template-rows: repeat(2, minmax(44px, 1fr)); column-gap: 14px; row-gap: 10px; min-height: 130px; padding: 16px; overflow: hidden; border: 1px solid color-mix(in srgb, var(--item-accent) 38%, rgba(255, 255, 255, 0.08)); border-left: 4px solid var(--item-accent); border-radius: 19px; color: #eef1ff; background: linear-gradient(90deg, color-mix(in srgb, var(--item-accent) 15%, transparent), transparent 56%), linear-gradient(135deg, rgba(15, 19, 46, 0.98), rgba(9, 11, 27, 0.98)), #0e1230; box-shadow: 0 0 18px color-mix(in srgb, var(--item-accent) 16%, transparent), 0 12px 28px rgba(0, 0, 0, 0.22); }
.shop-item-card .shop-item-rank { display: grid; grid-column: 1; grid-row: 1 / 3; width: 28px; height: 68px; place-items: center; align-self: center; border-radius: 11px; color: var(--item-accent); background: rgba(255, 255, 255, 0.06); font-size: 14px; font-weight: 950; }
.shop-item-card .shop-item-icon { grid-column: 2; grid-row: 1 / 3; width: 68px; height: 68px; align-self: center; font-size: 26px; }
.shop-item-card .shop-item-copy { grid-column: 3; grid-row: 1 / 3; align-self: center; gap: 5px; }
.shop-item-card .shop-item-copy strong { font-size: 19px; }
.shop-item-card .shop-item-copy small { color: rgba(238, 241, 255, 0.68); font-size: 12px; line-height: 1.35; }
.shop-item-card .shop-item-meta { display: grid; grid-column: 4; grid-row: 1; min-height: 44px; place-items: center; align-self: stretch; padding: 7px 9px; border: 1px solid color-mix(in srgb, var(--item-accent) 35%, rgba(255, 255, 255, 0.1)); border-radius: 14px; background: rgba(255, 255, 255, 0.06); }
.shop-item-card .shop-item-meta b { font-size: 14px; text-align: center; }
.shop-item-card .shop-item-action { grid-column: 4; grid-row: 2; width: 100%; min-height: 44px; align-self: stretch; border: 1px solid color-mix(in srgb, var(--item-accent) 56%, rgba(255, 255, 255, 0.16)); border-radius: 14px; color: #06101f; background: var(--item-accent); font-size: 14px; font-weight: 900; }
.shop-item-card .shop-item-action:disabled { cursor: not-allowed; color: rgba(238, 241, 255, 0.58); background: rgba(255, 255, 255, 0.09); box-shadow: none; }
.shop-item-card.owned { opacity: 0.78; }
.shop-item-card.featured { border-color: rgba(255, 227, 110, 0.6); box-shadow: 0 0 24px rgba(255, 227, 110, 0.16), 0 12px 28px rgba(0, 0, 0, 0.22); }

.shop-item-card.inventory { grid-template-columns: 22px 42px minmax(0, 1fr) 88px; grid-template-rows: auto auto; min-height: 72px; padding: 12px; border-left-width: 4px; }
.shop-item-card.inventory .shop-item-rank { width: 22px; height: auto; color: rgba(238, 241, 255, 0.44); font-size: 12px; font-weight: 900; }
.shop-item-card.inventory .shop-item-icon { width: 42px; height: 42px; font-size: 18px; }
.shop-item-card.inventory .shop-item-copy { grid-column: 3; grid-row: 1 / 3; }
.shop-item-card.inventory .shop-item-meta { grid-column: 4; grid-row: 1; min-height: auto; padding: 0; border: 0; background: transparent; }
.shop-item-card.inventory .shop-item-meta em { color: rgba(169, 182, 255, 0.9); font-size: 11px; font-style: normal; font-weight: 900; }
.shop-item-card.inventory .shop-item-meta b { color: #ffe09a; font-size: 11px; }
.shop-item-card.inventory .shop-item-action { grid-column: 4; grid-row: 2; min-height: 36px; border: 1px solid rgba(232, 241, 255, 0.5); color: #11182f; background: #dbe6f8; box-shadow: none; }
.shop-item-card.inventory .shop-item-action.is-equipped { border-color: rgba(169, 182, 255, 0.86); color: #050a1f; background: #a9b6ff; }
.shop-item-card.equipped { box-shadow: 0 0 0 1px rgba(169, 182, 255, 0.24) inset, 0 0 26px rgba(125, 147, 255, 0.22); }

.shop-item-icon { display: grid; width: 46px; height: 46px; place-items: center; border-radius: 50%; font-family: Georgia, serif; font-size: 20px; font-weight: 850; }
.shop-item-copy { display: grid; min-width: 0; gap: 4px; }
.shop-item-copy strong { overflow: hidden; font-size: 14px; text-overflow: ellipsis; white-space: nowrap; }
.shop-item-copy small { display: -webkit-box; overflow: hidden; color: rgba(238, 241, 255, 0.62); font-size: 10px; line-height: 1.4; -webkit-box-orient: vertical; -webkit-line-clamp: 2; }

/* 등급별 색상 아이콘/배지 (11단계 인디고→프리즘) */
.shop-skin-coral-spark { background: #ff6f59; color: #fff; }
.shop-skin-amber-glow { background: #ff9f43; color: #2a1300; }
.shop-skin-gold-ink { background: #ffd84f; color: #241900; }
.shop-skin-mint-page { background: #37d67a; color: #032013; }
.shop-skin-sky-notebook { background: #3aa7ff; color: #fff; }
.shop-skin-indigo-quill { background: #536dfe; color: #fff; }
.shop-skin-violet-verse { background: #b45cff; color: #fff; }
.shop-skin-prism-lexicon { background: conic-gradient(from 20deg, #ff6f59, #ff9f43, #ffd84f, #37d67a, #3aa7ff, #b45cff, #ff6f59); color: #111827; }
.shop-skin-globe-tongue { background: radial-gradient(circle at 35% 28%, #74d9ff 0 20%, transparent 21%), linear-gradient(135deg, #2f8cff, #35d0a8); color: #06251c; }
.shop-skin-sunrise-fluent { background: radial-gradient(circle, #fff5a8 0 19%, #ffbd3e 42%, #ff6f3c 100%); color: #2d1600; }
.shop-skin-nova-native { background: radial-gradient(circle, #05010c 0 34%, transparent 35%), conic-gradient(from 20deg, #2dd4bf, #8b5cf6, #f472b6, #2dd4bf); color: #fff; }
.shop-skin-classic { background: var(--primary); color: #fff; }
[class*="shop-skin-"] { box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.2) inset, 0 0 14px color-mix(in srgb, currentColor 30%, transparent); }

.leaderboard-row[class*="shop-skin-"] { border-width: 2px; border-style: solid; color: #fff; }
.leaderboard-row.shop-skin-coral-spark { border-color: #ff6f59; background: linear-gradient(110deg, #641521, #310b13 72%); }
.leaderboard-row.shop-skin-amber-glow { border-color: #ff9f43; background: linear-gradient(110deg, #71350d, #321506 72%); }
.leaderboard-row.shop-skin-gold-ink { border-color: #ffd84f; background: #241c07; }
.leaderboard-row.shop-skin-mint-page { border-color: #37d67a; background: #071f13; }
.leaderboard-row.shop-skin-sky-notebook { border-color: #3aa7ff; background: #07172c; }
.leaderboard-row.shop-skin-indigo-quill { border-color: #536dfe; background: #101332; }
.leaderboard-row.shop-skin-violet-verse { border-color: #b45cff; background: #1a0d2d; }
.leaderboard-row.shop-skin-prism-lexicon { border-color: #fff; background: #101225; box-shadow: 0 0 34px rgba(125, 147, 255, 0.3); }
.leaderboard-row.shop-skin-globe-tongue { border-color: #35d0a8; background: linear-gradient(110deg, #07342c, #0b1f3f 72%); }
.leaderboard-row.shop-skin-sunrise-fluent { border-color: #ffbd3e; background: linear-gradient(110deg, #5a2708, #261101 72%); }
.leaderboard-row.shop-skin-nova-native { border-color: #8b5cf6; background: radial-gradient(circle at 18% 50%, rgba(45, 212, 191, 0.18), transparent 28%), #090713; }
.leaderboard-row[class*="shop-skin-"] .rank-user strong, .leaderboard-row[class*="shop-skin-"] .rank-level { color: #fff; text-shadow: 0 1px 10px rgba(0, 0, 0, 0.32); }
.leaderboard-row[class*="shop-skin-"] .rank-user small { color: rgba(255, 255, 255, 0.72); }
.leaderboard-row[class*="shop-skin-"] .rank-number { color: #ffe36e; }

.leaderboard-screen { position: relative; padding-bottom: calc(104px + env(safe-area-inset-bottom)); }
.leaderboard-header { position: sticky; z-index: 4; top: 0; background: color-mix(in srgb, var(--background) 88%, transparent); backdrop-filter: blur(16px); }
.leaderboard-content { display: grid; width: 100%; max-width: 520px; gap: 14px; margin: 0 auto; padding: 22px 20px 30px; }
.my-rank-card { display: grid; gap: 3px; padding: 22px; border-radius: 26px; color: #fff; background: var(--primary); box-shadow: 0 16px 34px rgba(58, 92, 224, 0.26); }
.my-rank-card > span { color: rgba(255, 255, 255, 0.76); font-size: 11px; font-weight: 750; }
.my-rank-card > strong { font-size: 19px; }
.my-rank-card > strong b { font-size: 38px; }
.my-rank-card > small { color: rgba(255, 255, 255, 0.8); font-size: 11px; }
.leaderboard-note { padding: 12px 14px; border-radius: 15px; color: var(--text-soft); background: var(--surface-muted); font-size: 10px; line-height: 1.5; }
.leaderboard-list { display: grid; gap: 9px; }
.leaderboard-row { display: grid; grid-template-columns: 30px 42px 1fr auto; align-items: center; gap: 10px; padding: 13px 14px; border: 1px solid var(--line); border-radius: 19px; background: var(--surface); box-shadow: var(--shadow-soft); }
.leaderboard-row.me { border-color: color-mix(in srgb, var(--primary) 45%, var(--line)); background: color-mix(in srgb, var(--primary-soft) 45%, var(--surface)); }
.rank-number { color: var(--text-soft); font-size: 13px; font-weight: 900; text-align: center; }
.leaderboard-row:nth-child(1) .rank-number { color: #c98716; }
.leaderboard-row:nth-child(2) .rank-number { color: #7d8794; }
.leaderboard-row:nth-child(3) .rank-number { color: #a66b43; }
.rank-avatar { width: 42px; height: 42px; border-radius: 14px; font-size: 18px; }
.rank-user { display: grid; min-width: 0; gap: 3px; }
.rank-user strong { display: flex; min-width: 0; align-items: center; gap: 6px; overflow: hidden; font-size: 13px; text-overflow: ellipsis; white-space: nowrap; }
.rank-name-text { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.rank-user em { display: inline-block; margin-left: 4px; padding: 2px 5px; border-radius: 99px; color: var(--primary); background: var(--primary-soft); font-size: 8px; font-style: normal; vertical-align: 2px; }
.rank-user small { color: var(--text-soft); font-size: 10px; }
.rank-level { display: grid; justify-items: end; gap: 2px; color: var(--primary); font-size: 12px; font-weight: 850; }

.header-spacer { width: 44px; }
.mode-content { width: 100%; max-width: 520px; margin: 0 auto; padding: 50px 22px max(32px, env(safe-area-inset-bottom)); }
.lesson-symbol { display: grid; width: 72px; height: 72px; margin-bottom: 24px; place-items: center; border-radius: 23px; color: var(--primary); background: var(--primary-soft); font-family: Georgia, serif; font-size: 30px; font-weight: 700; }
.supporting-text { margin: 12px 0 0; color: var(--text-soft); font-size: 15px; line-height: 1.6; }
.mode-list { display: grid; gap: 14px; margin-top: 40px; font-size: 15px; }
.mode-card { position: relative; display: flex; width: 100%; align-items: center; gap: 15px; padding: 17px; border: 1px solid var(--line); border-radius: 22px; color: inherit; background: var(--surface); box-shadow: 0 6px 20px rgba(24, 32, 64, 0.035); text-align: left; }
.word-list-card { border-color: rgba(255, 138, 91, 0.2); background: radial-gradient(circle at 100% 0%, rgba(255, 138, 91, 0.12), transparent 45%), var(--surface); }
.mode-icon { display: grid; flex: 0 0 auto; width: 50px; height: 50px; place-items: center; border-radius: 16px; }
.ordered-icon { color: var(--primary); background: var(--primary-soft); }
.random-icon { color: #16a06a; background: #e8f8f1; }
.words-icon { color: var(--accent); background: var(--accent-soft); }
.mode-icon svg { width: 24px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }
.mode-copy { display: grid; flex: 1; gap: 5px; }
.mode-copy strong { font-size: 16px; }
.mode-copy small { color: var(--text-soft); font-size: 12px; }

.words-screen { background: radial-gradient(circle at 100% 0%, rgba(58, 92, 224, 0.1), transparent 26%), var(--background); }
.words-content { width: 100%; max-width: 520px; margin: 0 auto; padding: 26px 20px max(34px, env(safe-area-inset-bottom)); }
.words-heading { display: flex; align-items: center; gap: 17px; margin-bottom: 24px; }
.words-heading .lesson-symbol { flex: 0 0 auto; margin: 0; }
.words-heading h1 { margin: 0; font-size: 27px; letter-spacing: -0.03em; }
.words-heading .supporting-text { margin-top: 5px; font-size: 12px; }
.lesson-words-list { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 11px; }
.lesson-word-card { position: relative; display: grid; width: 100%; min-height: 118px; place-items: center; gap: 6px; padding: 18px 10px 14px; overflow: hidden; border: 1px solid var(--line); border-radius: 22px; color: var(--text); background: var(--surface); box-shadow: var(--shadow-soft); text-align: center; transition: border-color 140ms ease, transform 140ms ease; }
.lesson-word-card:hover { border-color: color-mix(in srgb, var(--primary) 42%, var(--line)); }
.lesson-word-card:active { transform: scale(0.98); }
.lesson-word-card::after { position: absolute; right: -15px; bottom: -18px; width: 52px; height: 52px; border-radius: 50%; background: var(--primary-soft); content: ""; }
.lesson-word-card .word-order { position: absolute; top: 11px; left: 12px; color: var(--text-soft); font-size: 9px; font-weight: 800; }
.lesson-word-card strong { position: relative; z-index: 1; font-family: Georgia, "Times New Roman", serif; font-size: 21px; line-height: 1.25; word-break: break-word; }
.lesson-word-card > span:last-child { position: relative; z-index: 1; color: var(--text-soft); font-size: 11px; font-weight: 700; text-align: center; }

.quiz-screen { position: relative; height: 100dvh; touch-action: pan-y; overflow: hidden; background: var(--background); }
.quiz-screen.swipe-ready { touch-action: none; }
.quiz-header { display: flex; align-items: center; gap: 12px; padding: max(15px, env(safe-area-inset-top)) 18px 12px; }
.close-button { background: transparent; }
.progress-track { flex: 1; height: 9px; overflow: hidden; border-radius: 99px; background: #e6e9ee; }
.progress-bar { width: 0; height: 100%; border-radius: inherit; background: var(--primary); transition: width 280ms ease; }
.quiz-count { min-width: 34px; color: var(--text-soft); font-size: 12px; font-weight: 750; text-align: right; }
.quiz-content { display: flex; flex: 1; min-height: 0; width: 100%; max-width: 520px; margin: 0 auto; flex-direction: column; padding: 10px 14px max(18px, env(safe-area-inset-bottom)); transition: transform 150ms ease, opacity 150ms ease; }
.quiz-reward-bar { display: grid; grid-template-columns: 1fr auto; gap: 10px; align-items: stretch; margin-bottom: 18px; }
.streak-fire { position: relative; display: flex; min-width: 0; align-items: center; gap: 11px; padding: 12px 13px; overflow: hidden; border: 1px solid var(--line); border-radius: 18px; color: var(--text-soft); background: var(--surface); box-shadow: 0 7px 18px rgba(24, 32, 64, 0.07); transition: border-color 160ms ease, background 160ms ease; }
.flame-orb { display: grid; flex: 0 0 auto; width: 50px; height: 50px; place-items: center; border-radius: 17px; background: #eef0f5; transition: background 180ms ease; }
.flame-orb svg { width: 34px; fill: #d5d6df; stroke: #a8aab7; stroke-width: 1.3; stroke-linecap: round; stroke-linejoin: round; transition: fill 180ms ease, stroke 180ms ease, filter 180ms ease; }
.streak-copy { display: grid; min-width: 0; flex: 1; gap: 1px; }
.streak-copy strong { color: var(--text); font-size: 16px; line-height: 1; }
.streak-copy strong b { margin-right: 3px; color: var(--accent); font-size: 28px; }
.streak-copy small { overflow: hidden; color: var(--text-soft); font-size: 10px; font-weight: 750; text-overflow: ellipsis; white-space: nowrap; }
.streak-next-reward { position: absolute; top: 9px; right: 10px; padding: 4px 7px; border-radius: 99px; color: #a96619; background: #fff1cf; font-size: 9px; font-weight: 900; }
.streak-fire.active { border-color: rgba(255, 138, 91, 0.28); color: #d86b37; background: #fff4ec; }
.streak-fire.active .flame-orb { background: #ffe0ca; }
.streak-fire.active .flame-orb svg { fill: #ffad65; stroke: #f0713d; filter: drop-shadow(0 3px 6px rgba(240, 113, 61, 0.25)); }
.streak-fire.combo { border-color: rgba(255, 106, 50, 0.38); background: #ffeadf; box-shadow: 0 8px 20px rgba(235, 104, 45, 0.12); }
.streak-fire.combo .flame-orb { background: #ff9c63; }
.streak-fire.combo .flame-orb svg { fill: #ff8a43; stroke: #fff3dd; }
.streak-fire.combo .streak-copy strong b { color: #e95424; }
.session-points { display: grid; min-width: 84px; place-items: center; align-content: center; gap: 5px; padding: 10px; border: 1px solid rgba(241, 163, 60, 0.18); border-radius: 18px; background: #fff7df; box-shadow: 0 7px 18px rgba(123, 83, 24, 0.07); text-align: center; }
.session-points > span:last-child { display: grid; gap: 1px; }
.session-points small { color: #9a7541; font-size: 8px; font-weight: 800; }
.session-points strong { color: #b56b16; font-size: 15px; }
.quiz-content.swipe-out { transform: translateX(-35%); opacity: 0; }

.study-card { position: relative; display: flex; min-height: clamp(210px, 31dvh, 290px); align-items: center; justify-content: center; flex-direction: column; overflow: hidden; padding: 28px 20px 24px; border: 1px solid rgba(58, 92, 224, 0.15); border-radius: 26px; background: var(--surface); box-shadow: 0 12px 28px rgba(24, 32, 64, 0.1); }
.study-card::before { position: absolute; top: 0; left: 0; width: 6px; height: 100%; background: var(--primary); content: ""; }
.study-card-heading { position: absolute; top: 18px; right: 18px; left: 20px; display: flex; align-items: center; justify-content: flex-start; }
.study-card-chip { padding: 7px 11px; border-radius: 99px; color: var(--primary); background: var(--primary-soft); font-size: 10px; font-weight: 800; }
.study-card-guide { margin: 34px 0 8px; color: var(--primary); font-size: 13px; font-weight: 800; }
.quiz-prompt-stack { display: grid; max-width: 100%; place-items: center; gap: 13px; }
.quiz-content h1 { margin: 5px 0 0; padding: 0 6px; font-size: clamp(28px, 8vw, 46px); letter-spacing: -0.03em; line-height: 1.3; text-align: center; word-break: keep-all; }
.quiz-content h1.word-prompt { font-family: Georgia, "Times New Roman", serif; font-style: italic; font-size: clamp(26px, 7.4vw, 42px); }
.current-reward { display: flex; align-items: center; gap: 7px; padding: 8px 13px 8px 9px; border: 1px solid rgba(241, 163, 60, 0.2); border-radius: 99px; color: #ae6817; background: #fff3d5; box-shadow: 0 7px 18px rgba(174, 104, 23, 0.1); font-size: 12px; font-weight: 850; white-space: nowrap; }
.current-reward .point-coin { width: 23px; height: 23px; font-size: 9px; }
.current-reward.earned { border-color: rgba(33, 161, 121, 0.2); color: var(--success); background: var(--success-soft); }
.quiz-hint { margin: 13px 0 0; color: var(--text-soft); font-size: 14px; text-align: center; }

.answer-list { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); flex: 1; grid-template-rows: repeat(2, minmax(92px, 1fr)); gap: 10px; margin-top: 16px; }
.answer-list[hidden] { display: none; }
.answer-button { min-height: 0; padding: 14px 10px; border: 2px solid color-mix(in srgb, var(--primary) 9%, var(--line)); border-radius: 20px; color: var(--text); background: var(--surface); font-family: inherit; font-size: clamp(14px, 4vw, 18px); font-weight: 750; line-height: 1.3; box-shadow: 0 7px 17px rgba(24, 32, 64, 0.065); transition: transform 120ms ease, border-color 120ms ease, background 120ms ease, box-shadow 120ms ease; word-break: keep-all; }
.answer-button:hover:not(:disabled) { border-color: color-mix(in srgb, var(--primary) 48%, var(--line)); box-shadow: 0 13px 29px rgba(24, 32, 64, 0.12); transform: translateY(-2px); }
.answer-button:disabled { cursor: default; pointer-events: none; }
.answer-button.correct { border-color: var(--success); color: var(--success); background: var(--success-soft); }
.answer-button.wrong { border-color: var(--danger); color: var(--danger); background: var(--danger-soft); }

.quiz-screen.typing-mode .quiz-content { overflow-y: auto; overscroll-behavior: contain; scroll-padding: 10px 0 calc(18px + var(--quiz-keyboard-inset, 0px)); -webkit-overflow-scrolling: touch; }
.quiz-screen.typing-keyboard-active { min-height: var(--quiz-visual-height, 100dvh); height: var(--quiz-visual-height, 100dvh); }
.quiz-screen.typing-keyboard-active .quiz-content { overflow-y: hidden; padding-bottom: max(10px, env(safe-area-inset-bottom)); }
.quiz-screen.typing-mode .study-card, .quiz-screen.typing-mode .typing-answer-form { flex: 0 0 auto; }

.typing-answer-form { display: grid; gap: 10px; margin-top: 16px; padding: 18px; border: 2px solid color-mix(in srgb, var(--primary) 13%, var(--line)); border-radius: 22px; background: var(--surface); box-shadow: var(--shadow-soft); }
.typing-answer-form[hidden] { display: none; }
.typing-answer-form > label { color: var(--text); font-size: 13px; font-weight: 850; }
.typing-answer-row { display: grid; grid-template-columns: 1fr auto; gap: 8px; }
.typing-answer-row input { min-width: 0; min-height: 58px; padding: 0 16px; border: 2px solid var(--line); border-radius: 16px; color: var(--text); outline: none; background: var(--surface-muted); font-size: 18px; font-weight: 750; transition: border-color 140ms ease, box-shadow 140ms ease; }
.typing-answer-row input:focus { border-color: var(--primary); box-shadow: 0 0 0 4px var(--primary-soft); }
.typing-answer-row button { min-width: 92px; border-radius: 16px; color: #fff; background: var(--primary); font-size: 12px; font-weight: 850; }
.typing-answer-form > small { color: var(--text-soft); font-size: 10px; }
.typing-answer-form.correct { border-color: var(--success); background: var(--success-soft); }
.typing-answer-form.wrong { border-color: var(--danger); background: var(--danger-soft); }
.typing-answer-form.correct input { border-color: var(--success); }
.typing-answer-form.wrong input { border-color: var(--danger); }
.typing-answer-form input:disabled, .typing-answer-form button:disabled { cursor: default; opacity: 0.72; pointer-events: none; }

.swipe-hint { width: 100%; margin: 22px 0 0; padding: 0; border: 0; color: var(--text-soft); background: transparent; font-size: 12px; line-height: 1.5; text-align: center; }
.swipe-hint.ready { color: var(--primary); font-weight: 700; }
.swipe-hint.typing-next { position: static; flex: 0 0 auto; min-height: 54px; margin-top: 14px; padding: 0 18px; border-radius: 16px; color: #fff; background: var(--primary); box-shadow: 0 10px 22px rgba(58, 92, 224, 0.22); font-size: 15px; font-weight: 850; }
.swipe-hint.typing-next:active { transform: translateY(1px); }

.result-screen { align-items: center; justify-content: flex-start; overflow-y: auto; padding: max(36px, env(safe-area-inset-top)) 22px max(28px, env(safe-area-inset-bottom)); }
.result-content { width: 100%; max-width: 460px; text-align: center; }
.result-badge { display: grid; width: 92px; height: 92px; margin: 0 auto 27px; place-items: center; border-radius: 30px; color: var(--success); background: var(--success-soft); transform: rotate(-4deg); }
.result-badge svg { width: 48px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
.result-content .eyebrow { margin-bottom: 8px; }
.score-card { display: grid; gap: 5px; margin: 35px 0 24px; padding: 25px; border-radius: 24px; background: var(--surface); box-shadow: var(--shadow); }
.score-card > span { color: var(--text-soft); font-size: 13px; }
.score-card > strong { color: var(--primary); font-size: 44px; letter-spacing: -0.03em; }
.score-details { display: flex; justify-content: center; gap: 26px; margin-top: 12px; padding-top: 17px; border-top: 1px solid var(--line); color: var(--text-soft); font-size: 13px; }
.score-details b { margin-left: 5px; color: var(--text); }
.result-actions { display: grid; gap: 11px; }
.result-point-card { display: grid; gap: 5px; margin: -10px 0 20px; padding: 18px; border: 1px solid rgba(241, 163, 60, 0.2); border-radius: 22px; background: #fff8e8; }
.result-point-card > span, .result-point-card small { color: #8b6b35; font-size: 11px; }
.result-point-card > strong { color: #b56b16; font-size: 28px; }
.wrong-answer-section { display: grid; gap: 10px; margin: -8px 0 24px; text-align: left; }
.wrong-answer-section[hidden] { display: none; }
.wrong-answer-section > header { display: flex; align-items: center; justify-content: space-between; }
.wrong-answer-section > header span { color: var(--text); font-size: 14px; font-weight: 850; }
.wrong-answer-section > header strong { color: var(--danger); font-size: 12px; }
.wrong-answer-list { display: grid; gap: 8px; }
.wrong-answer-row { display: grid; grid-template-columns: 76px 1fr; align-items: center; gap: 12px; padding: 12px; border: 1px solid color-mix(in srgb, var(--danger) 24%, var(--line)); border-radius: 18px; background: var(--surface); }
.wrong-answer-word { display: grid; min-height: 54px; place-items: center; padding: 6px; border-radius: 14px; color: var(--danger); background: var(--danger-soft); font-family: Georgia, "Times New Roman", serif; font-size: 14px; font-weight: 750; text-align: center; word-break: break-word; }
.wrong-answer-copy { display: grid; min-width: 0; gap: 4px; }
.wrong-answer-copy strong { color: var(--text); font-size: 13px; }
.wrong-answer-copy small { overflow-wrap: anywhere; color: var(--text-soft); font-size: 10px; line-height: 1.45; }

.purchase-modal { position: fixed; z-index: 100; inset: 0; display: flex; align-items: flex-end; justify-content: center; }
.purchase-modal[hidden] { display: none; }
.purchase-backdrop { position: absolute; inset: 0; background: rgba(10, 12, 22, 0.48); backdrop-filter: blur(4px); }
.purchase-sheet { position: relative; z-index: 1; width: min(100%, 430px); padding: 26px 22px max(22px, env(safe-area-inset-bottom)); border-radius: 30px 30px 0 0; color: var(--text); background: var(--surface); box-shadow: 0 -24px 70px rgba(0, 0, 0, 0.24); text-align: center; animation: sheet-in 220ms ease; }
@keyframes sheet-in { from { transform: translateY(20px); opacity: 0; } }
.purchase-symbol { display: grid; width: 68px; height: 68px; margin: 0 auto 17px; place-items: center; border-radius: 22px; font-family: Georgia, "Times New Roman", serif; font-size: 28px; font-weight: 750; }
.purchase-kicker { color: var(--primary); font-size: 11px; font-weight: 850; }
.purchase-sheet h2 { margin: 7px 0 7px; font-size: 25px; letter-spacing: -0.03em; }
.purchase-sheet > p { margin: 0 0 21px; color: var(--text-soft); font-size: 13px; }
.purchase-price-row { display: flex; align-items: center; justify-content: space-between; padding: 13px 15px; border-radius: 14px; background: var(--surface-muted); font-size: 12px; }
.purchase-price-row + .purchase-price-row { margin-top: 7px; }
.purchase-price-row strong { color: #b56b16; font-size: 14px; }
.purchase-price-row.balance strong { color: var(--text); }
.purchase-sheet .primary-button { margin-top: 17px; }
.attendance-sheet { padding-top: 30px; }
.attendance-reward-icon { display: grid; width: 70px; height: 70px; margin: 0 auto 17px; place-items: center; border-radius: 23px; color: #a56613; background: #fff0cf; }
.attendance-reward-icon svg { width: 34px; fill: none; stroke: currentColor; stroke-width: 1.8; stroke-linecap: round; stroke-linejoin: round; }
.attendance-reward-points { display: flex; min-height: 66px; align-items: center; justify-content: center; gap: 10px; border: 1px solid rgba(241, 163, 60, 0.2); border-radius: 19px; color: #a56613; background: #fff8e8; font-size: 25px; }

.word-preview-modal { position: fixed; z-index: 130; inset: 0; display: flex; justify-content: center; background: rgba(10, 12, 22, 0.58); backdrop-filter: blur(8px); }
.word-preview-modal[hidden] { display: none; }
.word-preview-shell { display: flex; width: min(100%, 430px); min-height: 100dvh; flex-direction: column; overflow: hidden; color: var(--text); background: var(--background); animation: word-preview-in 180ms ease; }
@keyframes word-preview-in { from { transform: translateY(12px); opacity: 0; } }
.word-preview-header { display: grid; grid-template-columns: 44px 1fr auto; align-items: center; gap: 10px; padding: max(14px, env(safe-area-inset-top)) 18px 10px; }
.word-preview-header > strong { font-size: 14px; text-align: center; }
.word-preview-header > span { min-width: 44px; color: var(--text-soft); font-size: 11px; font-weight: 750; text-align: right; }
.word-preview-stage { display: flex; min-height: 0; flex: 1; flex-direction: column; justify-content: center; padding: 18px 20px; touch-action: pan-y; user-select: none; }
.word-preview-guide { margin: 0 0 13px; color: var(--text-soft); font-size: 11px; font-weight: 700; text-align: center; }
.word-preview-card { position: relative; display: grid; min-height: min(52dvh, 440px); place-content: center; gap: 22px; padding: 38px 24px; overflow: hidden; border: 1px solid var(--line); border-radius: 32px; background: var(--surface); box-shadow: var(--shadow); text-align: center; }
.word-preview-card::after { position: absolute; right: -72px; bottom: -85px; width: 190px; height: 190px; border-radius: 50%; background: var(--primary-soft); content: ""; }
.word-preview-order { position: absolute; z-index: 1; top: 22px; left: 24px; color: var(--text-soft); font-size: 12px; font-weight: 850; }
.word-preview-card h2 { position: relative; z-index: 1; margin: 0; padding: 0 12px; font-family: Georgia, "Times New Roman", serif; font-style: italic; font-size: clamp(32px, 9vw, 54px); font-weight: 700; line-height: 1.15; word-break: break-word; }
.word-preview-card p { position: relative; z-index: 1; margin: 0; color: var(--text-soft); font-size: clamp(17px, 5vw, 22px); font-weight: 800; }
.word-preview-controls { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; padding: 8px 20px max(20px, env(safe-area-inset-bottom)); }
.word-preview-controls button { display: flex; min-height: 54px; align-items: center; justify-content: center; gap: 7px; border-radius: 17px; color: var(--primary); background: var(--primary-soft); font-size: 13px; font-weight: 850; }
.word-preview-controls button:last-child { color: #fff; background: var(--primary); }
.word-preview-controls button:disabled { cursor: not-allowed; color: var(--text-soft); background: var(--surface-muted); opacity: 0.55; }
.word-preview-controls svg { width: 18px; fill: none; stroke: currentColor; stroke-width: 2; stroke-linecap: round; stroke-linejoin: round; }

.primary-button:disabled { cursor: not-allowed; opacity: 0.46; box-shadow: none; }
.sheet-cancel-button { width: 100%; min-height: 46px; margin-top: 7px; border-radius: 15px; color: var(--text-soft); background: transparent; font-size: 13px; font-weight: 750; }
.modal-open { overflow: hidden; }

.app-toast { position: fixed; z-index: 150; bottom: calc(94px + env(safe-area-inset-bottom)); left: 50%; max-width: calc(100% - 40px); padding: 12px 17px; border: 1px solid rgba(255, 255, 255, 0.14); border-radius: 99px; color: #fff; background: rgba(20, 22, 36, 0.92); box-shadow: 0 12px 34px rgba(0, 0, 0, 0.22); transform: translate(-50%, 18px); opacity: 0; pointer-events: none; font-size: 12px; font-weight: 750; text-align: center; transition: transform 180ms ease, opacity 180ms ease; }
.app-toast.show { transform: translate(-50%, 0); opacity: 1; }

:root[data-theme="dark"] .login-screen { background: radial-gradient(circle at 20% 4%, rgba(255, 159, 120, 0.12), transparent 28%), linear-gradient(160deg, #171a28 0%, #1e2340 46%, #10131f 46%); }
:root[data-theme="dark"] .input-field > span:first-child { color: var(--text-soft); }
:root[data-theme="dark"] .input-field input { color: var(--text); background: var(--surface); }
:root[data-theme="dark"] .floating-card { border-color: rgba(255, 255, 255, 0.08); background: rgba(35, 40, 60, 0.68); }
:root[data-theme="dark"] .point-pill, :root[data-theme="dark"] .lesson-status.price, :root[data-theme="dark"] .result-point-card { border-color: rgba(241, 163, 60, 0.18); color: #f5c272; background: #30291f; }
:root[data-theme="dark"] .textbook-card { background: radial-gradient(circle at 95% 0%, rgba(255, 159, 120, 0.12), transparent 35%), linear-gradient(145deg, #1f2233, #232a4a); }
:root[data-theme="dark"] .book-tags span { background: rgba(255, 255, 255, 0.04); }
:root[data-theme="dark"] .streak-icon { background: rgba(255, 255, 255, 0.06); }
:root[data-theme="dark"] .lesson-card.purchasable { background: linear-gradient(135deg, var(--surface), #2e2820); }
:root[data-theme="dark"] .streak-fire.active, :root[data-theme="dark"] .streak-fire.combo { border-color: rgba(255, 159, 120, 0.2); color: #ffad81; background: #382620; }
:root[data-theme="dark"] .streak-fire { border-color: var(--line); background: radial-gradient(circle at 0% 0%, rgba(255, 159, 120, 0.08), transparent 36%), var(--surface); }
:root[data-theme="dark"] .flame-orb { background: #292c3e; }
:root[data-theme="dark"] .streak-fire.active .flame-orb { background: #653724; }
:root[data-theme="dark"] .streak-fire.combo .flame-orb { background: #d86a3f; }
:root[data-theme="dark"] .streak-next-reward { color: #f3bc68; background: #3a3020; }
:root[data-theme="dark"] .session-points { border-color: rgba(241, 163, 60, 0.14); background: #30291f; }
:root[data-theme="dark"] .session-points small { color: #d0b27f; }
:root[data-theme="dark"] .session-points strong { color: #f3bc68; }
:root[data-theme="dark"] .progress-track { background: #292d3d; }
:root[data-theme="dark"] .current-reward { border-color: rgba(241, 163, 60, 0.18); color: #f3bc68; background: #30291f; }
:root[data-theme="dark"] .current-reward.earned { border-color: rgba(69, 201, 150, 0.18); color: var(--success); background: var(--success-soft); }
:root[data-theme="dark"] .result-point-card > span, :root[data-theme="dark"] .result-point-card small { color: #d5b476; }
:root[data-theme="dark"] .result-point-card > strong, :root[data-theme="dark"] .purchase-price-row strong { color: #f3bc68; }
:root[data-theme="dark"] .attendance-card { border-color: rgba(241, 163, 60, 0.16); background: #30291f; }
:root[data-theme="dark"] .attendance-icon { color: #f3bc68; background: #493821; }
:root[data-theme="dark"] .attendance-card span { color: #d0b27f; }
:root[data-theme="dark"] .attendance-card button:disabled { color: #b89d70; background: #493d2b; }
:root[data-theme="dark"] .attendance-reward-icon { color: #f3bc68; background: #493821; }
:root[data-theme="dark"] .attendance-reward-points { border-color: rgba(241, 163, 60, 0.16); color: #f3bc68; background: #30291f; }

@media (max-width: 360px) {
    .top-bar { align-items: flex-start; gap: 8px; }
    .top-bar h1 { max-width: 160px; font-size: 21px; }
    .home-actions { gap: 5px; }
    .point-pill { padding-right: 9px; }
    .point-coin { width: 24px; height: 24px; }
    .profile-summary-card { gap: 9px; padding: 13px; }
    .profile-avatar { width: 42px; height: 42px; border-radius: 14px; font-size: 19px; }
    .level-up-button { min-width: 64px; padding-inline: 7px; }
    .attendance-card { grid-template-columns: auto 1fr; }
    .attendance-card button { grid-column: 1 / -1; width: 100%; }
    .word-preview-stage { padding-inline: 14px; }
    .word-preview-card { min-height: min(50dvh, 380px); border-radius: 27px; }
    .word-preview-controls { padding-inline: 14px; }
    .textbook-card { grid-template-columns: 92px 1fr; gap: 14px; padding: 14px; }
    .shop-content { padding-inline: 14px; }
    .shop-wallet-panel { align-items: flex-start; flex-direction: column; }
    .shop-view-tabs button { min-height: 40px; }
    .shop-item-card { grid-template-columns: 22px 56px minmax(0, 1fr) 92px; grid-template-rows: repeat(2, minmax(40px, 1fr)); column-gap: 9px; row-gap: 8px; min-height: 112px; padding: 12px; }
    .shop-item-card .shop-item-icon { width: 56px; height: 56px; font-size: 22px; }
    .shop-item-card .shop-item-copy strong { font-size: 16px; }
    .shop-item-card .shop-item-copy small { font-size: 11px; }
    .book-meta h2 { font-size: 19px; }
    .book-meta p { font-size: 11px; }
    .book-tags span { padding: 4px 6px; font-size: 9px; }
    .answer-list { grid-template-rows: repeat(2, minmax(80px, 1fr)); }
}

@media (min-width: 600px) {
    body { padding: 28px 0; background: #e9edf5; }
    .app-shell { max-width: 430px; min-height: calc(100dvh - 56px); margin: 0 auto; border-radius: 34px; box-shadow: 0 30px 80px rgba(18, 24, 40, 0.14); }
    .screen { min-height: calc(100dvh - 56px); }
    .quiz-screen { height: calc(100dvh - 56px); }
    .purchase-sheet { margin-bottom: 28px; border-radius: 30px; }
}
:root[data-theme="dark"] body { background: #0a0c14; }

@media (max-height: 700px) {
    .login-screen { align-items: center; }
    .floating-card { opacity: 0.45; }
    .login-content h1 { font-size: 29px; }
    .login-form { margin-top: 24px; }
    .input-field input, .primary-button { min-height: 52px; height: 52px; }
    .quiz-content { padding: 6px 12px 10px; }
    .quiz-reward-bar { gap: 7px; margin-bottom: 8px; }
    .streak-fire { gap: 8px; padding: 7px 9px; border-radius: 16px; }
    .flame-orb { width: 40px; height: 40px; border-radius: 13px; }
    .flame-orb svg { width: 28px; }
    .streak-copy strong { font-size: 14px; }
    .streak-copy strong b { font-size: 23px; }
    .session-points { min-width: 72px; padding: 7px; border-radius: 16px; }
    .study-card { min-height: 170px; padding: 22px 16px 17px; border-radius: 22px; }
    .study-card-heading { top: 12px; left: 15px; }
    .study-card-guide { margin-top: 25px; font-size: 11px; }
    .quiz-content h1 { font-size: clamp(22px, 6.5vw, 34px); }
    .quiz-prompt-stack { gap: 7px; }
    .current-reward { padding: 5px 10px 5px 6px; font-size: 10px; }
    .quiz-hint { margin-top: 7px; font-size: 11px; }
    .answer-list { grid-template-rows: repeat(2, minmax(70px, 1fr)); gap: 8px; margin-top: 10px; }
    .answer-button { border-radius: 17px; font-size: clamp(12px, 3.4vw, 15px); }
    .typing-answer-form { gap: 8px; margin-top: 10px; padding: 14px; border-radius: 18px; }
    .quiz-screen.typing-keyboard-active .quiz-reward-bar { display: none; }
    .quiz-screen.typing-keyboard-active .study-card { min-height: 148px; padding: 16px 16px 12px; }
    .quiz-screen.typing-keyboard-active .study-card-guide { margin: 10px 0 4px; font-size: 11px; }
    .quiz-screen.typing-keyboard-active .quiz-content h1.word-prompt { font-size: clamp(24px, 7vw, 36px); line-height: 1.15; }
    .quiz-screen.typing-keyboard-active .current-reward, .quiz-screen.typing-keyboard-active .typing-answer-form > small, .quiz-screen.typing-keyboard-active .swipe-hint { display: none; }
    .quiz-screen.typing-keyboard-active .quiz-hint { display: none; }
    .quiz-screen.typing-keyboard-active .typing-answer-form { margin-top: 10px; padding: 12px; }
    .quiz-screen.typing-keyboard-active .typing-answer-row input { min-height: 50px; font-size: 16px; }
    .typing-answer-row { grid-template-columns: 1fr; }
    .typing-answer-row input { min-height: 50px; font-size: 16px; }
    .typing-answer-row button { min-height: 46px; }
    .swipe-hint:not(.ready) { display: none; }
}

@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { scroll-behavior: auto !important; animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
}